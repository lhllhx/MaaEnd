# MapNavigator 与 MapTracker 在线路 2 自动采集中的对比研究

## 短结论

两种算法目前都已经相当成熟，能够稳定完成大部分导航任务。在常规场景下，二者差距并不算特别大：

- `MapNavigator` 更快，更适合日常导航和强调轮转速度的场景。
- `MapTracker` 命中率更高，更适合强调单轮收益和单点采集成功率的场景。

本次测试中，两种算法都没有出现掉下桥或掉下悬崖的情况，整体表现都值得肯定。

## 前言

`MapNavigator` 和 `MapTracker` 现阶段都已经比较成熟。本文的目的是提供一份研究报告，讨论两种算法在实际使用中的适用情形，供开发者在设计任务时进行选择。

本次研究选择了自动采集线路 2 作为测试对象。之所以选择这条路线，是因为它包含一大段桥梁，并且我在中间额外放置了一个电线杆作为路障，以提高测试难度。每种算法均运行 5 次，观察其在该路线上的运行时间与采集命中情况。

实验配置如下：

- `MapTracker` 开启了 `AllTargets` 参数，以保证其定位精度。由于大部分路径位于桥上，如果不开启该参数，算法较容易掉下桥。
- `MapNavigator` 仅在最后一个点位和采集点开启严格到达点声明。
- 路线录制均使用各自配套的 GUI 工具直接打点，没有额外进行人工修饰。
- 所有相关代码均已公开：<https://github.com/lhllhx/MaaEnd/tree/csgo>

## 统计口径

为避免事件分发导致的重复日志干扰，本报告采用以下统计口径：

- `总时长`
  从 `Tasker.Task.Starting` 到 `Tasker.Task.Succeeded` 的时间差。
- `移动点数`
  每次路线固定经过 9 个 `GotoFind` 点位。
- `采集次数`
  定义为 `AutoCollectClick` 成功触发 `ClickKey` 的次数。也就是说，这里统计的是“成功识别到采集物并实际执行采集”的次数，而不是单纯 OCR 识别事件数。
- `命中率`
  `采集次数 / 9`
- `平均单段移动耗时`
  从每个 `GotoFind` 的 `Node.Action.Starting` 到 `Node.Action.Succeeded` 的平均耗时。

该口径更接近实际业务效果，也更适合作为路线算法优劣判断依据。

## 单次运行结果

| 任务 ID | 线路 | 算法 | 开始时间 | 结束时间 | 总时长(s) | 移动点数 | 采集次数 | 命中率 | 平均单段移动(ms) |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| 200000001 | 4 | MapTracker | 20:33:12.488 | 20:35:00.266 | 107.778 | 9 | 6 | 66.67% | 8811.0 |
| 200000002 | 4 | MapTracker | 20:35:00.280 | 20:36:49.286 | 109.006 | 9 | 7 | 77.78% | 8920.0 |
| 200000003 | 4 | MapTracker | 20:36:49.288 | 20:38:35.157 | 105.869 | 9 | 5 | 55.56% | 8740.9 |
| 200000004 | 4 | MapTracker | 20:38:35.181 | 20:40:20.979 | 105.798 | 9 | 7 | 77.78% | 8602.6 |
| 200000005 | 4 | MapTracker | 20:40:20.980 | 20:42:11.887 | 110.907 | 9 | 8 | 88.89% | 9062.2 |
| 200000006 | 2 | MapNavigator | 20:42:11.916 | 20:43:49.776 | 97.860 | 9 | 4 | 44.44% | 7947.4 |
| 200000007 | 2 | MapNavigator | 20:43:49.778 | 20:45:11.282 | 81.504 | 9 | 4 | 44.44% | 6103.0 |
| 200000008 | 2 | MapNavigator | 20:45:11.284 | 20:46:36.617 | 85.333 | 9 | 5 | 55.56% | 6441.6 |
| 200000009 | 2 | MapNavigator | 20:46:36.647 | 20:47:59.335 | 82.688 | 9 | 5 | 55.56% | 6004.3 |
| 200000010 | 2 | MapNavigator | 20:47:59.337 | 20:49:23.291 | 83.954 | 9 | 7 | 77.78% | 6126.6 |

## 汇总结果

| 线路 | 算法 | 样本数 | 平均总时长(s) | 时长范围(s) | 总采集次数 | 平均采集次数 | 平均命中率 | 平均单段移动(ms) |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 2 | MapNavigator | 5 | 86.268 | 81.504 - 97.860 | 25 | 5.0 | 55.56% | 6524.6 |
| 4 | MapTracker | 5 | 107.872 | 105.798 - 110.907 | 33 | 6.6 | 73.33% | 8827.3 |

## 结果分析

### 1. 运行时间

从总时长看，线路 2 的 `MapNavigator` 明显更快。

- `MapNavigator` 平均总时长：`86.268s`
- `MapTracker` 平均总时长：`107.872s`

两者相差：

- `21.604s`
- 约 `25.0%`

这说明在当前实现下，`MapNavigator` 的整体路径执行效率更高，完成一轮任务所需时间明显更短。

### 2. 采集命中情况

从成功识别并执行采集动作的次数看，线路 4 的 `MapTracker` 更优。

- `MapNavigator`
  平均每轮采集 `5.0` 次，平均命中率 `55.56%`
- `MapTracker`
  平均每轮采集 `6.6` 次，平均命中率 `73.33%`

差值为：

- 平均每轮多采 `1.6` 个点
- 命中率高 `17.77` 个百分点

这说明 `MapTracker` 在停靠点落位、视角保持或路径贴合资源点方面更有优势，能够更稳定地触发采集。

### 3. 移动效率

从平均单段移动耗时看，线路 2 的 `MapNavigator` 同样更快。

- `MapNavigator`：`6524.6ms`
- `MapTracker`：`8827.3ms`

`MapTracker` 单段平均慢约：

- `2302.7ms`
- 约 `35.3%`

这与总时长差异是吻合的，说明 `MapTracker` 的主要代价来自每个点位之间的移动更慢，而不是额外流程开销。

### 4. 稳定性

从时长波动范围看：

- `MapTracker`：`105.798s - 110.907s`
- `MapNavigator`：`81.504s - 97.860s`

可以看出：

- `MapTracker` 的时长更稳定，波动较小
- `MapNavigator` 的时长下限更低，第一个轮次明显偏慢的情况是因为MapTracker将其设定为走路模式而MapNavigator没有将其切换为跑路模式，笔者发现后手动切换

因此可以简单概括为：

- `MapTracker` 更偏“稳定但慢”
- `MapNavigator` 更偏“整体更快”

## 优缺点分析

### MapNavigator

优点：

- 单轮运行更快，平均耗时更低
- 单段移动耗时更短，路线节奏更紧凑
- 更适合追求快速轮转的使用场景

缺点：

- 采集命中率偏低
- 部分轮次仅采到 `4/9`
- 对初始状态、局部路径偏差或落点误差可能更敏感

### MapTracker

优点：

- 采集命中率更高
- 多数轮次可采到 `6` 个以上点位，最好达到 `8/9`
- 总体时长波动较小，表现更稳定

缺点：

- 单轮运行明显更慢
- 单段移动开销更高
- 在强调单位时间轮转次数的场景下不占优势

## 结论

本次 10 次样本对比可以得出以下结论：

首先，两种算法都没有在测试中掉下桥或掉下悬崖，这一点非常值得肯定。

`MapNavigator` 的优势在于速度。由于 `MapTracker` 在本次测试中必须开启更严格的参数设置以保证桥面上的稳定性，`MapNavigator` 平均快了约 `21.6s`，更适合作为强调效率和轮转速度的方案，适用于日常导航。

`MapTracker` 的优势在于采集覆盖率。受益于更精细的识别与定位能力，它平均每轮比 `MapNavigator` 多采 `1.6` 个点，命中率高约 `17.77` 个百分点，更适合作为强调单轮收益的方案，适用于单点采集或希望尽量减少漏采的场景。

简而言之：

- 如果更看重速度，选 `MapNavigator`
- 如果更看重命中率和覆盖率，选 `MapTracker`

## 研究局限性

本研究也存在一些局限：

- 仅研究了一条路线，无法覆盖全部场景
- 没有研究跨地区导航场景
- 本次“采集”实际上是只识别不真正采集，因此可能存在残留采集物未消失、从而导致误识别的情况，进而夸大采集结果

因此，本文更适合作为路线相对表现的参考，而不是对全部场景下绝对性能的最终结论。

## 展望

最后，也记录一下在测试过程中的一些主观感受和后续期望：

- 两种算法的 GUI 界面目前都没有提供便捷的“仅捕获当前坐标”选项，导致打点过程比较痛苦，希望后续可以补上
- 希望 `MapNavigator` 进一步提升定位精度，以实现更好的落点控制。首次运行应自动切换为跑路模式，不然就一路走过去了。
- 希望 `MapTracker` 进一步优化控制算法，在不开启更严格参数的情况下也能维持较好的定位精度，从而提升整体效率

